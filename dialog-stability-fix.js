"use strict";

// The reserved state is handled by the CTA and its contextual WhatsApp
// message. Do not inject additional dialog content into the observed DOM.
updateReservedDialogNote = function disableReservedDialogNote() {};
